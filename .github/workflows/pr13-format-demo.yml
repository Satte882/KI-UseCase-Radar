name: PR13 Format Demo Architecture

on:
  push:
    branches: [feature/delivery-handover]

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/delivery-handover
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - uses: astral-sh/setup-uv@v6
        with:
          version: "0.10.0"
          enable-cache: true
      - run: uv sync --frozen --dev
      - name: Apply Ruff formatter
        run: uv run ruff format ki_radar/core/demo_architecture_data.py
      - name: Commit formatting if needed
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add ki_radar/core/demo_architecture_data.py
          git commit -m "Apply Ruff formatting to demo architecture data"
          git push origin HEAD:feature/delivery-handover
