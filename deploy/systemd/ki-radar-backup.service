[Unit]
Description=KI-Radar database backup
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
WorkingDirectory=/srv/ki-radar
ExecStart=/usr/bin/docker compose -f compose.prod.yml --profile jobs run --rm backup
